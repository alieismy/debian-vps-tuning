# v0.1.0-rc.9（发布草案）

这是 `debian-vps-tuning` 首发候选版本的第九轮变更，新增安全总控入口、512 MiB profile、按资源约束的缓冲/journald、3X-UI NOFILE 预置、常规 `pfifo_fast` 回滚和只读诊断，并把现有 2G 档位扩展为支持 1–2 vCPU。状态 schema 保持为 3。

## 新增总控入口

- 新增 `debian-vps-tuning.sh`；
- 自动识别 Debian 12/13、amd64 和 1C512MB、1C1GB、1C2GB、2C2GB 四个资源档；
- 2G 档保留 `1c2g` 文件名和 profile ID，1C2GB/2C2GB 共用实现以兼容已有状态；
- 带宽默认 200 Mbps，允许 100–1000 Mbps；
- 默认安全引导先执行 preflight，只有再次明确确认才 apply；
- 支持 `preflight`、`apply`、`verify`、`status`、只读 `diagnose`、`rollback` 和高级 `recover`；
- 512M/1G/2G socket 上限分别为 16/32/64 MiB；预置 `x-ui.service` 的 `LimitNOFILE=65536`；
- 支持常规默认根 `pfifo_fast` 的快照、切换、恢复和后置语义验证；
- 本地和远程 profile 均在调用前核对唯一 SHA-256 清单条目；
- 远程模式固定到 `v0.1.0-rc.9`，只允许 HTTPS，不回退可变分支、latest 或第三方源；
- 保留底层 profile 的原始输出和退出码。

## Release assets

发布 draft 必须先上传并校验：

- `debian-vps-tuning.sh`；
- `debian12-1c512m-vps-tuning.sh`；
- `debian12-1c1g-vps-tuning.sh`；
- `debian12-1c2g-vps-tuning.sh`；
- `debian13-1c512m-vps-tuning.sh`；
- `debian13-1c1g-vps-tuning.sh`；
- `debian13-1c2g-vps-tuning.sh`；
- `SHA256SUMS`。

全部资产上传前不得发布；发布后不得移动 tag 或替换同名资产。若仓库支持 Release immutability，应在创建 rc.9 Release 前启用，并按“draft → 上传资产 → 校验 → 发布”的顺序操作。

当前发布候选校验和：

| 文件 | SHA-256 |
|---|---|
| `SHA256SUMS` | `3d399ef1ceb347982fa50a939d0947dd9addd0394e386a3f6b65e6d2b98a7309` |
| `debian-vps-tuning.sh` | `0fa951046f5d83b7fbacee08c6365e1a175af42bf6d8594fb01e2c2c0262f14a` |
| `debian12-1c512m-vps-tuning.sh` | `6a5a3c92c2e794cabb2df3c10619434c5bafd85bbd63c412ef8a7a3a55c4b52a` |
| `debian12-1c1g-vps-tuning.sh` | `f2ff280d6d4bcec3dcacc010b83e3fcd8790ab3a69964dad9c1b72d54f200cb4` |
| `debian12-1c2g-vps-tuning.sh` | `def079ee0e98296d38de3267246d4ee5b82e1e151691bd788b8717fdf0f200d2` |
| `debian13-1c512m-vps-tuning.sh` | `ac6793738a5038d40773189593bbfe2181f004e912efec43587a6ad51228a190` |
| `debian13-1c1g-vps-tuning.sh` | `9c73d95ee9512624ad508c5d9986e3169173b555b8614a4036ac8c5ba4d33c58` |
| `debian13-1c2g-vps-tuning.sh` | `3b115f73e82589251be8d32a3798e039d80abc32c74b5f4041951f10f15f3f86` |

## 验证

- Bash 语法检查通过；
- ShellCheck v0.11.0 通过；
- Debian 12/13、1C512MB、1C1GB、1C2GB 和 2C2GB 档位 fixture 通过；2C512MB、2C1GB、3C2G 和超范围内存按预期拒绝；
- 1536 MiB 边界、200 Mbps 默认值和 100–1000 Mbps 范围测试通过；
- 清单重复、哈希不匹配、下载失败和底层退出码透传测试通过；
- 六份 profile 与公共模板生成一致；
- 既有状态机和 qdisc 故障注入测试通过。

## 发布边界

rc.9 是预发行版。固定 Release assets 的真实下载只有发布后才能验证；交互确认、总控入口、512 MiB、2C2G、Debian 13、常规 `pfifo_fast` 和 1/3/5/10 并发业务矩阵仍需目标机证据。rc.8 的运行证据不能替代 rc.9 总控入口和 profile 版本的实机复测，因此不得标记为正式 `v0.1.0`。
