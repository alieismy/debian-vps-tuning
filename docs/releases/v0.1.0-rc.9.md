# v0.1.0-rc.9（Pre-release）

这是 `debian-vps-tuning` 首发候选版本的第九轮变更，新增安全总控入口、512 MiB profile、按资源约束的缓冲/journald、3X-UI NOFILE 预置、常规 `pfifo_fast` 回滚和只读诊断，并把现有 2G 档位扩展为支持 1–2 vCPU。状态 schema 保持为 3。

## 新增总控入口

- 新增 `debian-vps-tuning.sh`；
- 自动识别 Debian 12/13、amd64 和 1C512MB、1C1GB、1C2GB、2C2GB 四个资源档；
- 2G 档保留 `1c2g` 文件名和 profile ID，1C2GB/2C2GB 共用实现以兼容已有状态；
- 带宽默认 200 Mbps，允许 100–1000 Mbps；
- 默认安全引导先执行 preflight，只有再次明确确认才 apply；
- 支持 `preflight`、`apply`、`verify`、`status`、只读 `diagnose`、`rollback` 和高级 `recover`；
- 512M/1G/2G socket 上限分别为 16/32/64 MiB；预置 `x-ui.service` 的 `LimitNOFILE=65536`；
- 支持常规默认根 `pfifo_fast` 的快照、切换、恢复和后置语义验证；
- 本地和远程 profile 均在调用前核对唯一 SHA-256 清单条目；
- 远程模式固定到 `v0.1.0-rc.9`，只允许 HTTPS，不回退可变分支、latest 或第三方源；
- 保留底层 profile 的原始输出和退出码。
- swap purge 通过同目录临时文件和原子替换更新 `/etc/fstab`，过滤或替换失败时保留原文件并返回失败；
- 已安装状态下重复 `apply` 且未显式给出带宽时，复用状态中的端口带宽。

## Release assets

已发布并校验以下固定资产：

- `debian-vps-tuning.sh`；
- `debian12-1c512m-vps-tuning.sh`；
- `debian12-1c1g-vps-tuning.sh`；
- `debian12-1c2g-vps-tuning.sh`；
- `debian13-1c512m-vps-tuning.sh`；
- `debian13-1c1g-vps-tuning.sh`；
- `debian13-1c2g-vps-tuning.sh`；
- `SHA256SUMS`。

八个资产已从公开 Release 下载地址重新下载并通过 `SHA256SUMS`。发布后不得移动 tag 或替换同名资产；发现缺陷时发布新版本。当前仓库未启用 GitHub Release immutability。

当前发布候选校验和：

| 文件 | SHA-256 |
|---|---|
| `SHA256SUMS` | `142ab89eb1eba1d92dc9ca5e9e9149d9f453e0d7c7e48bb33a7a718e97500f21` |
| `debian-vps-tuning.sh` | `09cbb77591760fa1789729c31f64e03b29f145f50c8c419bca6057b23f492979` |
| `debian12-1c512m-vps-tuning.sh` | `6d597157a9995c6f1576b1648e94f51d91f3ea73792d9f3b3733e0767bc1e3e5` |
| `debian12-1c1g-vps-tuning.sh` | `f927b838f9aac213816d13a32cdc6b39dc6844ce97c9076e0dbabc0b5d4e5cfc` |
| `debian12-1c2g-vps-tuning.sh` | `6b69e9e759420b94b1280de9095ee1c696e9d89b68fdeb7d2313c15460eaaddd` |
| `debian13-1c512m-vps-tuning.sh` | `22ca56e9f34811127864b3db798cbd96e8467cb1eda6292a3cde0482575a0e0d` |
| `debian13-1c1g-vps-tuning.sh` | `9fd70c593b83d41337c6dfcada737855ce583e8bc3451ee8bb5952db850c81c6` |
| `debian13-1c2g-vps-tuning.sh` | `cdf2e45ccbab86ae599ad02872d8f3a3bd33ae0f84e48a02f5465cb5a1313032` |

## 验证

- Bash 语法检查通过；
- ShellCheck v0.11.0 通过；
- Debian 12/13、1C512MB、1C1GB、1C2GB 和 2C2GB 档位 fixture 通过；2C512MB、2C1GB、3C2G 和超范围内存按预期拒绝；
- 1536 MiB 边界、200 Mbps 默认值和 100–1000 Mbps 范围测试通过；
- 清单重复、哈希不匹配、下载失败和底层退出码透传测试通过；
- 六份 profile 与公共模板生成一致；
- 既有状态机和 qdisc 故障注入测试通过；
- `/etc/fstab` 过滤/替换失败保护和已安装非默认带宽的重复 `apply` fixture 通过；
- Debian 13、1C1G、1000 Mbps、ext4 目标机已通过固定 Release 联网下载、安全引导、首次 apply、安装 3X-UI 前重启验证、安装后严格验证及再次重启后的严格验证；`x-ui.service` 主进程和直接 Xray 子进程 NOFILE 均为 65536/65536。

## 发布边界

rc.9 是预发行版。上述实机只覆盖 Debian 13、1C1G、1000 Mbps、ext4 的主路径；该机的无显式端口重复 apply、rollback/purge、VLESS + REALITY + TCP 客户端连通性和 1/3/5/10 并发性能尚未完成。512 MiB、2G、2C2G、Debian 12 及其他带宽组合仍需目标机证据，因此不得标记为正式 `v0.1.0`。
